spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  datasource:
   url: jdbc:mysql://localhost:3306/segomagazine
   username: root
   password: 123456
  flyway:
    baseline-on-migrate: true

