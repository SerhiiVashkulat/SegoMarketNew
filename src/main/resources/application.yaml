spring:
  main:
    allow-circular-references: true
  jpa:
    hibernate:
      ddl-auto: validate
      use-new-id-generator-mappings: false
    show-sql: true
  datasource:
   url: jdbc:mysql://localhost:3306/segomarket?useSSL=false
   username: root
   password: 123456
